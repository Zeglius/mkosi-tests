#!/usr/bin/bash
# This script is used to generate the version string for the mkosi

set -e ${DEBUG:+-x}

MAYOR_VERSION_FALLBACK=41

OSTREE_VERSION=$(
    source mkosi.basetree/usr/lib/os-release || true
    echo ${OSTREE_VERSION:-$MAYOR_VERSION_FALLBACK}
)
echo $OSTREE_VERSION
